# Force npm usage (not pnpm)
engine-strict=false
legacy-peer-deps=true

# Registry
registry=https://registry.npmjs.org/

# For better-sqlite3 compilation
# Allow scripts during install
ignore-scripts=false
